[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/woning/:ref"
  to = "/.netlify/functions/share-page?ref=:ref"
  status = 200
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
